[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "ce-yt-dlp"
version="1.0.0"
description = "A yt-dpl plugin package (ce)."
readme = { file = "README.md", content-type = "text/markdown" }
classifiers = [
  "License :: OSI Approved :: GNU General Public License v3 (GPLv3)"
]
license = { text = "GPL-3.0-only" }
requires-python = ">=3.10"
keywords = ["yt-dlp", "yt-dlp-plugin"]
maintainers = [
  { name = "tfkxsx", email = "tfkxsx@gmail.com" },
]
dependencies = [
    "yt-dlp",
    "psutil>=5.9.0"
]

[tool.hatch.build.targets.wheel]
packages = ["plugin/yt_dlp_plugins"]

[tool.hatch.build.targets.sdist]
include = [
  "README.md",
  "LICENSE",
  "pyproject.toml",
  "plugin/yt_dlp_plugins/**",
]

[tool.hatch.metadata]
allow-direct-references = true

[tool.ruff.format]
quote-style = "single"

[tool.ruff]
line-length = 120
